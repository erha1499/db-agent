"""Shared interpretation defaults, never SQL policy or execution authorization."""

QUERY_CONVENTIONS = """
常规明细浏览约定（主生成、需求提取和最终复核一致使用）：
用户明确的表、业务范围、字段、事件、筛选和排序，以及适用的已确认业务知识优先。
仅浏览某类实体的明细、没有指定表或特殊业务范围时，优先选择名称直接对应实体、
无额外业务前后缀的基础表；其他带前后缀的同类表不因此成为额外查询目标。
必须取得所选表的实际结构；不存在唯一的直接对应表、或业务范围确实冲突时仍需澄清。
“最近/最新 N 条实体”默认按实际结构中明确的创建时间字段（如 created_at）降序、
LIMIT N，不用 id 代替时间，不额外添加日期窗口、状态筛选或支付时间条件。
若用户明确支付、更新等事件，则使用该事件对应字段，不能改成创建时间。
缺少可对应的时间字段或多个创建时间字段无法确定时仍是缺口；并列时间未要求次序、
未指定时间窗口或时区，不妨碍按已有时间值排序取前 N 条。
明细浏览未指定返回列时，可列出该表实际结构中的普通明细字段；不能因未点名字段而拒绝。
这些默认选择不是未决问题，不写入 uncertainties 或据此返回 uncertain；实际 SQL 展示所用口径。
默认约定不能补造指标定义、表关系或缺失结构，不能覆盖原样 SQL、放宽权限或绕过执行预检。
"""

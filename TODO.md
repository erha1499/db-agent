# db-agent 大方向 TODO

目标：让研发人员通过自然语言或 SQL，完成有明确权限、风险检查和可核对结果的数据库查询与分析，再按实际需求扩展业务知识和受控变更。

**当前位置：第13步「持续集成与可复现交付」已完成，下一步是第14步「业务知识与跨会话记忆」。CLI 多轮查询和本机 Web 页面已可使用，详情见 [Web 使用说明](docs/web.md)；自动回归已在真实 GitHub CI 通过。提供方探针重现返回计数超过请求上限，硬 token 上限、计费及服务器取消仍未验证，不代表生产可靠性保证。**

本清单维护大方向、推进顺序和完成状态。`[x]` 表示所述范围已实现并有验证记录，不代表通用 SQL 支持或生产环境验收；具体配置、命令和能力限制见 [README](README.md)，开发约定见 [AGENTS.md](AGENTS.md)。

## 阶段总览

| 阶段 | 大步骤 | 状态 | 阶段目标 |
| --- | --- | --- | --- |
| 基础接入 | 1–6：初始化、配置、LLM、框架、数据库、元数据 | 已完成 | Agent 能调用工具，理解授权库表的结构 |
| 受控查询 | 7–10：静态校验、计划诊断、查询执行、测试与记录 | 已完成 | SQL 经检查后执行，结果与失败有依据可查 |
| 业务可用 | 11：业务验收与回答可靠性 | 已完成约定场景验收 | SQL 完整表达用户要求，回答与实际业务结果一致 |
| 连续使用与交付 | 12–13：多轮查询、持续集成 | 已完成约定范围验收 | 支持连续追问，让后续改动有自动回归保障 |
| 按需求扩展 | 14–19：记忆、优化、分析、服务化、变更、数据源 | 后续候选 | 根据实际使用中的需求选择下一项能力 |

## 已完成：建立基础查询闭环

- [x] **1. 项目初始化**：建立 Python 项目、依赖管理、CLI 入口、Git 仓库和基础文档。入口：[pyproject.toml](pyproject.toml)。
- [x] **2. 配置与密钥管理**：模型、数据库及预算统一从 `.env` 配置，真实配置不进入版本控制，提供占位模板。入口：[配置模块](src/db_agent/config.py)、[.env.example](.env.example)。
- [x] **3. 接入 LLM**：接入单个 OpenAI 兼容模型，支持配置校验、真实连通性检查和模型请求预算。入口：[模型接入](src/db_agent/agent.py)。
- [x] **4. 接入 Agent 框架与工具调用**：使用 LangChain 管理模型与领域工具的调用、参数校验、结果回填和停止条件；每轮运行具有独立预算和连接。入口：[Agent](src/db_agent/agent.py)、[工具定义](src/db_agent/tools.py)。
- [x] **5. 连接数据库**：建立本地 MySQL 8.4 数据源、只读账号、表白名单和合成业务数据，支持独立连接检查；另有百万订单及客户、商品、明细、支付和退款数据的分批导入。入口：[Compose](compose.yaml)、[连接器](src/db_agent/db.py)、[电商数据](docs/ecommerce.md)。
- [x] **6. 获取数据库元数据**：提供 `list_tables`、`describe_table`，读取授权基础表的字段、索引和同库授权表之间声明的完整外键列对，为模型编写与解释 SQL 提供依据。入口：[元数据工具](src/db_agent/tools.py)。
- [x] **7. SQL 静态校验**：通过 MySQL 语法树检查单语句、操作类型、表权限和支持范围；拒绝危险操作，未知语法不自动放行。入口：[静态规则](src/db_agent/policy.py)。
- [x] **8. EXPLAIN 计划诊断**：获取普通执行计划，按扫描、连接和排序等证据判断风险，提供 `analyze_sql` 和结构化诊断报告。入口：[计划规则](src/db_agent/plans.py)、[分析服务](src/db_agent/analysis.py)。
- [x] **9. 受控执行 SQL**：提供 `execute_query` 和 `db query`；执行入口重新预检，仅对通过检查的 SELECT 使用只读事务执行，限制时间和结果大小。入口：[查询服务](src/db_agent/query.py)、[结果处理](src/db_agent/results.py)。
- [x] **10. 基础测试与运行记录**：建立离线规则和协议测试、显式运行的真实 MySQL 测试及脱敏运行事件，支持定位失败并回归验证。入口：[测试目录](tests/)、[运行记录](src/db_agent/records.py)。

当前已打通「理解请求 → 查看结构 → 生成 SQL → 预检与计划诊断 → 受控查询 → 返回结果」。执行链路跑通不等于生成的 SQL 完整符合用户业务要求，后者由第 11 步单独验收；本地事件记录也不等于完整审计或查询结果持久化。

## 已完成：让业务结果和回答可验收

- [x] **11. 业务验收与回答可靠性**：建立百万订单场景、独立预期与固定案例；通过完整需求合同、最终复核和实际表关系修复已知漏筛选、退款关联与计数问题。查询回答可信展示 SQL、结果、截断和失败，已完成回归与独立新题验收。模型超时和协议失败仍可能使任务未完成，不能计为查数成功；提供方 token 计数与请求上限的关系尚未核实。入口：[需求核对](docs/semantic-review.md)、[评测说明](evals/README.md)、[运行与验证](docs/ecommerce.md)。

v1–v7 已用于开发或诊断，后续重跑明确标为回归；v8 在首次执行前冻结旧题与独立新题，并完成预定验收。原退款参考 SQL 的风险拒绝保留，Agent 原业务题用实际生成的完整关联与结果另行验收；不放宽阈值、不修改旧预期。正式失败与定向诊断分别记录，后者不替换前者。完整案例、首次失败和各类验证结果维护在[电商业务验收记录](docs/ecommerce.md)，本清单不重复维护测试数字。

完成标志：约定任务可以复跑，结果有独立预期，已知业务失败已修复并通过回归与独立验收。保留失败记录，不通过修改既有预期或放宽 SQL 风险规则取得通过结果。

## 已完成：连续使用与可复现交付

- [x] **12. 会话内多轮查询**：支持「查二月成交额 → 按客户拆分 → 与一月比较」等连续追问，每轮重新取得结构并完整预检。有界用户请求上下文和 `session` CLI 已实现；失败、取消或结果不完整后必须重置。已完成真实跨月比较、筛选替换、空集后追问和切换主题验收，首次模型超时与追加回归分别记录。CLI 仅保留进程内用户请求；Web 的本机历史快照与业务口径继承分开，不提供跨会话业务记忆。入口：[会话说明](docs/conversations.md)、[验收记录](evals/CONVERSATIONS.md)。
- [x] **13. 持续集成与可复现交付**：Python 3.11/3.13 回归、真实 MySQL 集成、前端构建/浏览器合同、锁定依赖与打包安装已接入并通过实际 GitHub CI；前端下载地址使用经完整性核对的公开 registry，产物排除本地配置和运行数据。安装、配置和逐层目标验收有可复现入口；显式提供方探针验证普通/工具协议与客户端超时，并重现计数超请求的异常，硬 token/计费/服务器取消边界保留。真实模型评测不自动运行。入口：[交付与提供方验收](docs/delivery.md)、[CI](.github/workflows/ci.yml)。

## 后续候选方向：按实际需求选择

以下不是必须按顺序全部完成的承诺，也不因列入清单而自动启动。

- [ ] **14. 业务知识与跨会话记忆**：保存经确认的指标口径、表关系和 SQL 模板，注明来源、适用数据源和失效条件；按需要引入持久化，记忆不携带执行授权。
- [ ] **15. SQL 优化验证**：在已有诊断和候选建议基础上，补充结果等价验证与优化效果证据；改写前后使用独立基准和边界数据核对。
- [ ] **16. 数据分析与结果交付**：支持趋势、分组对比、图表和报告；出现导出或再次读取结果的需求时，再建设受控的结果保存与取回能力。
- [ ] **17. 服务化与使用环境接入**：已接入本机单用户 Web 页面、FastAPI、历史和进度/停止，整合验收记录见 [Web 使用说明](docs/web.md)；面向多用户开放前同步建立可信身份、数据权限、隔离和模型数据使用边界，再验收目标环境。
- [ ] **18. 受控数据库变更**：从明确限定的小范围变更开始，加入前后值预览、审批、执行前状态检查、事务与恢复机制，以及提交结果不确定时的核对流程。
- [ ] **19. 扩展 SQL 与数据源支持**：根据真实任务扩展当前 SQL 子集；出现第二种数据库需求后，再增加方言、连接器和对应验证，不沿用未经验证的放行规则。

## 人与 Agent 如何使用这份清单

1. 开始任务时先确认所属大步骤、当前能力和本次验收范围；默认围绕「下一步」推进，用户明确指定其他方向时按指定任务执行。
2. 把本次实现细项放在具体任务中，本文件只保留大步骤；列入 TODO 不代表已经授权实施、提交或发布。
3. 实现与必要验证完成后再勾选，同时更新顶部「当前位置」；未完成的部分明确写出，不把计划写成现有能力。
4. 大方向和状态以本清单为入口，运行方式维护在 README，开发边界维护在 AGENTS.md；改变方向时同步核对三者的一致性。

# 项目 TODO

目标：让研发人员通过自然语言或 SQL，完成有明确权限、风险检查和可核对结果的数据库查询与分析，再按实际需求扩展业务知识和受控变更。

**当前位置：第 11 步进行中。百万订单数据、可重复业务评测与可信结果展示已落地；下一优先是修复模型偶发遗漏业务筛选条件，随后再推进多轮查询。**

本清单维护大方向、推进顺序和完成状态。`[x]` 表示所述范围已实现并有验证记录，不代表通用 SQL 支持或生产环境验收；具体配置、命令和能力限制见 [README](README.md)，开发约定见 [AGENTS.md](AGENTS.md)。

## 已完成：建立基础查询闭环

- [x] **1. 项目初始化**：建立 Python 项目、依赖管理、CLI 入口、Git 仓库和基础文档。入口：[pyproject.toml](pyproject.toml)。
- [x] **2. 配置与密钥管理**：模型、数据库及预算统一从 `.env` 配置，真实配置不进入版本控制，提供占位模板。入口：[配置模块](src/db_agent/config.py)、[.env.example](.env.example)。
- [x] **3. 接入 LLM**：接入单个 OpenAI 兼容模型，支持配置校验、真实连通性检查和模型请求预算。入口：[模型接入](src/db_agent/agent.py)。
- [x] **4. 接入 Agent 框架与工具调用**：使用 LangChain 管理模型与领域工具的调用、参数校验、结果回填和停止条件；当前每次运行是独立会话。入口：[Agent](src/db_agent/agent.py)、[工具定义](src/db_agent/tools.py)。
- [x] **5. 连接数据库**：建立本地 MySQL 8.4 数据源、只读账号、表白名单和合成业务数据，支持独立连接检查；另有百万订单及客户、商品、明细、支付和退款数据的分批导入。入口：[Compose](compose.yaml)、[连接器](src/db_agent/db.py)、[电商数据](docs/ecommerce.md)。
- [x] **6. 获取数据库元数据**：提供 `list_tables`、`describe_table`，读取授权基础表的字段和索引，为模型编写与解释 SQL 提供依据。入口：[元数据工具](src/db_agent/tools.py)。
- [x] **7. SQL 静态校验**：通过 MySQL 语法树检查单语句、操作类型、表权限和支持范围；拒绝危险操作，未知语法不自动放行。入口：[静态规则](src/db_agent/policy.py)。
- [x] **8. EXPLAIN 计划诊断**：获取普通执行计划，按扫描、连接和排序等证据判断风险，提供 `analyze_sql` 和结构化诊断报告。入口：[计划规则](src/db_agent/plans.py)、[分析服务](src/db_agent/analysis.py)。
- [x] **9. 受控执行 SQL**：提供 `execute_query` 和 `db query`；执行入口重新预检，仅对通过检查的 SELECT 使用只读事务执行，限制时间和结果大小。入口：[查询服务](src/db_agent/query.py)、[结果处理](src/db_agent/results.py)。
- [x] **10. 基础测试与运行记录**：建立离线规则和协议测试、显式运行的真实 MySQL 测试及脱敏运行事件；已验证少量真实模型调用案例。入口：[测试目录](tests/)、[运行记录](src/db_agent/records.py)。

当前已打通「理解请求 → 查看结构 → 生成 SQL → 预检与计划诊断 → 受控查询 → 返回结果」。少量模型案例跑通不等于回答可靠性已验收；本地事件记录也不等于完整审计或查询结果持久化。

## 下一步：让业务结果和回答可验收

- [ ] **11. 业务验收与回答可靠性（当前优先）**：已建立百万订单电商场景、公开业务字典、独立预期结果、开发回归与冻结案例，以及 SQL/结果/截断/失败的可信展示。当前剩余问题是模型偶发漏掉用户要求的筛选条件，仍需修复并验收。入口：[评测说明](evals/README.md)、[运行与验证](docs/ecommerce.md)。

这一阶段已交付可重复运行的评测脚本、固定案例和失败报告。2026-09-10 本地验证：832 项测试通过，v2 SQL 24/24，真实模型开发回归 31/32、新冻结集 16/16（各两轮）。开发回归中一次「只查零订单客户」漏掉聚合后的筛选，返回了全部客户；因此本步骤保持未完成。不同集合、重复次数及测试类型分别报告，不合并成生产正确率。

下一次优先复现并处理这类业务语义遗漏，保留首次失败。修复后用已暴露案例做回归，再设计未参与调整的新验收题；不要放宽 SQL 风险规则，也不要为得到通过结果改写既有预期。当前先完成订单统计链路，不建设通用评测平台。

完成标志：固定任务可以复跑，关键事实有独立依据，已知失败有修复和回归证据，剩余问题明确列出。不能只凭单元测试数量或另一个模型的判断勾选完成。

## 随后推进：支持连续使用

- [ ] **12. 会话内多轮查询**：支持「查二月成交额 → 按客户拆分 → 与一月比较」等连续追问，正确延续和修改业务口径；每次新 SQL 仍重新预检。
- [ ] **13. 持续集成与可复现交付**：把必要的自动回归接入 CI，完善安装、配置与目标环境验收流程；真实模型评测保持显式运行。此项可配合第 11 步推进，当前已有本地测试，尚未建立 CI 工作流。

## 后续候选方向：按实际需求选择

以下不是必须按顺序全部完成的承诺，也不因列入清单而自动启动。

- [ ] **14. 业务知识与跨会话记忆**：保存经确认的指标口径、表关系和 SQL 模板，注明来源、适用数据源和失效条件；按需要引入持久化，记忆不携带执行授权。
- [ ] **15. SQL 优化验证**：在已有诊断和候选建议基础上，补充结果等价验证与优化效果证据；改写前后使用独立基准和边界数据核对。
- [ ] **16. 数据分析与结果交付**：支持趋势、分组对比、图表和报告；出现导出或再次读取结果的需求时，再建设受控的结果保存与取回能力。
- [ ] **17. 服务化与使用环境接入**：按需要提供 API 或页面；面向多用户开放前同步建立可信身份、数据权限、隔离和模型数据使用边界，再验收目标环境。
- [ ] **18. 受控数据库变更**：从明确限定的小范围变更开始，加入前后值预览、审批、执行前状态检查、事务与恢复机制，以及提交结果不确定时的核对流程。
- [ ] **19. 扩展 SQL 与数据源支持**：根据真实任务扩展当前 SQL 子集；出现第二种数据库需求后，再增加方言、连接器和对应验证，不沿用未经验证的放行规则。

## 人与 Agent 如何使用这份清单

1. 开始任务时先确认所属大步骤、当前能力和本次验收范围；默认围绕「下一步」推进，用户明确指定其他方向时按指定任务执行。
2. 把本次实现细项放在具体任务中，本文件只保留大步骤；列入 TODO 不代表已经授权实施、提交或发布。
3. 实现与必要验证完成后再勾选，同时更新顶部「当前位置」；未完成的部分明确写出，不把计划写成现有能力。
4. 大方向和状态以本清单为入口，运行方式维护在 README，开发边界维护在 AGENTS.md；改变方向时同步核对三者的一致性。

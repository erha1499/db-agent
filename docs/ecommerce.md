# 电商数据与业务验收

目标是让查询能力在有规模和业务边界的电商数据上接受验收：客户下单、订单包含多条商品明细、支付可能失败或拆分、退款可能部分完成。所有数据均由公开代码生成，不包含真实用户资料。

## 数据规模与业务关系

默认配置为 `orders=1000000`、`customers=100000`、`products=10000`、`seed=20260910`，数据版本为 `ecommerce-v1`。同一配置产生相同数据，生成器逐单输出，导入器每批默认处理 1000 个订单及其子记录。

| 表 | 默认实际行数 | 含义 |
| --- | ---: | --- |
| `ec_customers` | 100,000 | 客户、地区、注册时间；包含没有订单的客户和 NULL 地区 |
| `ec_products` | 10,000 | 商品、分类、当前价格及停售时间 |
| `ec_orders` | 1,000,000 | 订单状态、成交毛额、创建/支付/取消时间 |
| `ec_order_items` | 2,998,854 | 订单商品、数量、成交单价、优惠及明细净额 |
| `ec_payments` | 1,426,667 | 支付尝试、成功到账和失败原因；一单可多次支付 |
| `ec_refunds` | 572,360 | 针对具体支付的退款尝试与完成时间；一笔支付可多次退款 |

六张业务表合计 6,107,881 行。订单通过 `customer_id` 关联客户；明细通过 `order_id`、`product_id` 关联订单与商品；支付关联订单；退款同时通过订单和支付的复合外键关联原支付。全部使用 InnoDB，主键、外键和状态/金额 CHECK 在导入期间保持启用。

背景订单覆盖 2026 年第一季度，包含热点客户、热点商品和 2 月 15–17 日活动集中下单；普通背景单有 2–4 条明细。固定的低 ID 边界订单独立于背景数据，包含跨月支付、零元订单、取消/待支付、拆分支付和部分退款。背景客户 `1000` 另有可由公式独立推导的 1000 笔、100000.00 元订单，用于在百万订单表上验证索引查询。

金额采用人民币元，生成时按整数分计算，存储为 DECIMAL。订单金额为各明细 `quantity * unit_price - discount_amount` 的合计；成功支付、成功退款分别按流水状态与各自完成时间统计。按创建月、支付月和退款月统计是三种口径，订单数也不等于支付流水数。具体边界账本和独立预期值见 [评测说明](../evals/README.md)。合成数据约定按 UTC 解释时间，但 MySQL DATETIME 本身不携带时区。

## 生成与导入

先按 [README](../README.md) 配置 `.env` 并启动本地 Compose MySQL。导入命令使用容器内 root socket，只允许本项目健康的 `db-agent/mysql`、`db_agent` 库和 `127.0.0.1:13306` 绑定；root 不进入 Agent 配置。

```bash
# 默认实际导入 100 万订单及关联数据。
uv run python scripts/seed_ecommerce.py --apply

# 只生成可查看的 SQL 文件，不访问数据库；目标文件必须尚不存在。
uv run python scripts/seed_ecommerce.py --sql-output outputs/ecommerce/ecommerce.sql

# 对现有数据重新核对清单、真实行数和金额关系，不修改数据。
uv run python scripts/seed_ecommerce.py --verify-only
```

首次导入也可同时指定 `--apply --sql-output outputs/ecommerce/ecommerce.sql`。可以调整 `--orders`、`--customers`、`--products`、`--seed` 与 `--batch-size` 生成其他规模；固定评测要求默认百万规模，其他规模不能使用该预期值声明通过。

脚本取得同连接的导入锁，确认六张业务表和 `ec_seed_manifest` 均不存在后建表。客户/商品与各订单批次分别提交，已提交的数据不会因后续批次失败被抹去。清单先记录 `LOADING`，只有真实行数、订单/明细/支付/退款关系验证通过，并完成统计信息采集后才记录 `COMPLETE`。同配置的完整数据再次 `--apply` 只验收；部分表、不同配置或未完成状态会停止，不会覆盖、DROP、自动重试或断点补写。失败后需先核对清单与现场，再明确决定如何处理。

导出的 SQL 没有运行 Python 的数据库验收，因此文件末尾仅标记 `UNVERIFIED`；直接执行该文件不能作为验收通过证据。对导出数据使用 `--verify-only` 可以生成独立验收报告，但不会改写其数据库清单状态。推荐使用 `--apply` 完成导入与验收闭环。

导入报告和 SQL 只写入 Git 忽略的 `outputs/ecommerce/`。管理清单不加入 Agent 白名单；导入器不是 Agent 工具，也不接受任意 SQL 或数据库目标。

## 开放只读查询

在 `.env` 原白名单中追加六张业务表，保留原有三个小表：

```dotenv
DB_AGENT_MYSQL_ALLOWED_TABLES=["customers","orders","order_items","ec_customers","ec_products","ec_orders","ec_order_items","ec_payments","ec_refunds"]
```

```bash
uv run db-agent db describe ec_orders
uv run db-agent db query "SELECT COUNT(*) AS n, SUM(total_amount) AS amount FROM ec_orders WHERE customer_id=1000 AND created_at >= '2026-02-15' AND created_at < '2026-02-16'"
uv run db-agent chat '查询 ec_orders 中 customer_id=1000 在2026年2月15日创建的订单笔数和 total_amount 合计，日期范围左闭右开。'
```

订单索引覆盖客户与创建时间、状态与创建时间，以及支付时间；支付和退款也有订单/状态及完成时间索引。业务查询仍走原有静态预检、普通 EXPLAIN 与受控 SELECT，不放宽扫描阈值。跨全表的大聚合可能进入 `REVIEW`，只返回少量结果或添加 LIMIT 都不能绕过它。

## 可重复验收

```bash
uv run python scripts/evaluate_ecommerce.py --suite v8 --mode sql --split dev --repeat 1
uv run python scripts/evaluate_ecommerce.py --suite v8 --mode sql --split holdout --repeat 1
uv run python scripts/evaluate_ecommerce.py --suite v8 --mode agent --split dev --repeat 1
uv run python scripts/evaluate_ecommerce.py --suite v8 --mode agent --split holdout --repeat 2
```

SQL 模式直接调用受控查询服务；Agent 模式向配置模型发送业务问题及公开业务字典，让模型自行调用工具和生成 SQL，不向模型提供参考 SQL 或预期值。字典明确成功支付/退款使用 `succeeded` 等状态映射，避免把未知业务口径当成模型应当猜出的知识。两种模式都按独立预期值核对实际结果；Agent 模式还核对动作数量、预算和最终回答的可信报告展示路径。每题即时显示状态，完整报告位于 `outputs/evals/`；有失败时退出码为 1，配置问题为 2。

数据版本始终为 `ecommerce-v1`；评测套件另外分版本。v1–v7 的 64 道题已用于诊断框架步数、筛选遗漏、语义协议、计划兼容、关联信息、元数据发现轮次或数值表示，保留原题与全部历史报告，不再将后续重跑当作未见过的验收。当前 [v8](../evals/V8.md) 原样保留这 64 题作 dev，另有 8 道独立新题。共有字典只含口径与状态，不含答案。报告绑定套件、实际输入、源码哈希、模型配置及每次尝试。

开放式解释质量不由本评测自动打分；修改冻结用例或根据其失败调整实现后，需要新版本重新验收。生产运行事件仍不保存原始 SQL 或业务值，显式合成评测的报告可以保留查询证据用于定位失败。

上述行数来自本地 MySQL 8.4.11 的真实导入和重新验收。它验证当前合成数据上的业务结果与控制边界，不构成生产部署、TB 级压测或通用查询正确率结论。

## 2026-09-10 首次数据导入与业务评测

本次实际导入 1110 个事务批次，并重新核对六表真实行数、订单与明细金额、成功支付金额、退款归属及每笔支付的累计退款上限。六表统计信息采集均返回 `OK`；重复 `--apply` 确认已有数据后只做验收，未重复写入。

| 验证 | 本次结果 | 范围 |
| --- | --- | --- |
| 完整 pytest，开启两个 MySQL 集成开关 | 832 项通过 | 含 41 项真实 MySQL 集成；其余为离线规则、框架、生成器和失败路径测试 |
| Ruff / Git diff 检查 | 通过 | 静态检查与变更格式 |
| v2 固定 SQL | dev 16/16，holdout 8/8 | 真实受控查询；每题一轮 |
| v2 真实模型回归 | 31/32 | 16 个已暴露案例，各两轮 |
| v2 新冻结模型验收 | 16/16 | 8 道未参与本次提示调整的新题，各两轮 |

模型回归失败发生在第二轮「只查询零订单客户」：SQL 包含 LEFT JOIN 与 COUNT，但缺少聚合后的零值筛选，返回了全部八位客户。返回数据与该 SQL 一致，回答展示也忠实于工具报告，但未满足用户的业务条件。当时该问题尚未解决，随后作为 [TODO 第 11 步](../TODO.md) 的修复目标；静态预检的授权与风险判断不能替代自然语言业务正确性验收。

此前 v1 模型开发集首次 14/16、首次冻结集 12/16 的失败也保留在本机报告中。由此修正了框架步数计算、SQL 支持范围说明与结果直接展示的产品契约，并在 v2 明确业务字典。没有修改旧报告或原题答案，也没有调整数据库扫描阈值。当时的 31/32 与 16/16 是不同集合上的有限样本结果，不能据此声称生产正确率。

本次模型使用本机配置的别名，提供方未提供可核验的实际权重版本；具体配置、来源哈希、逐题输入哈希、运行 ID 和失败记录保留在 `outputs/evals/`。SQL 导出与原始评测报告不纳入公开提交。直接数据库查询不依赖模型，真实环境上线和开放式解释质量尚未验收。


## 查询需求核对的开发与失败记录

本轮针对“只找没有订单的客户，却返回全部客户”加入[独立需求合同与最终复核](semantic-review.md)。原始失败报告 `d146e5b62c1d456483a2bc66843e75ba`、题目、参考 SQL 与 `[[4,0],[8,0]]` 的人工预期保持不变。先独立提取完整查询要求，代码编译并选定完整 SQL，再复核并走原执行器；不按题目 ID 写入答案，也不在结果返回后补过滤。

开发阶段没有只保留通过轮次：

| 阶段 / 本机报告 ID | 实际结果与用途 |
| --- | --- |
| 单独语义审查 `c6b14481add34c1683d3b42095a6d675` | 26 道正反例各 2 次，检出 50/52；原始漏 HAVING 候选两次都被误判 match。证明单次审查不足，随后加入无候选输入的 QueryIntent。修正建议复核 22/22 是另一分母，不代表完成查询。 |
| 初版完整合同 `eae65716a075484499a181d65776d0f2`、`901840e1fec24a0ab9847d2994fe62a8` | v2 旧题分别 25/32 与 14/16；包含把实现说明误当需求缺口、中文 SQL 别名、引用拼接、结构协议与超时失败，全部未派发的失败保留。 |
| 历史坏候选重放 `0eb58b7cb9374ca88b13f62f3075341d`、`72278e217d4c4b9e8f388cb3c6daade8` | 两个源码版本各预定 10 次，分别完成 5/10、8/10。13 次完成均自动形成带零订单筛选的完整 SQL，只返回客户4、8；其余7次没有执行业务SQL；较早5次只留下通用失败码及约30秒耗时，后一次报告的2次明确为TIMEOUT，不能从旧错误码补造更精细分类。主生成节点为历史 fixture，意图/复核与MySQL为真实调用，不算完整真实生成评测。 |
| 后续 v3 旧回归 `83b9759bef2e451b906a2fbdc925fa7f` | 24 题各 2 次，46/48；两次是需求引用或复核协议无效，未派发。所有完成的结果均符合预期，仍将两次未完成计为失败。 |
| v3 首轮新题 `a1f6e2a9e9de4013886337e16d1faf28`、`f88405d02e5347d69685530ef528188d` | 固定 SQL 6/8，Agent 12/16；两条 DESC 查询因计划中的 `backward_index_scan` 未支持而 UNKNOWN。原题、原预期和失败保持原样。 |
| 计划兼容修复后的 v3 回归 `b5e193b12c594ea380090f7b50d0645b`、`79cdcd8331e1483ebd37483562a8485e` | 固定 SQL 8/8，Agent 15/16；剩余一次是拼接原文引用导致精确来源校验失败，未派发。v3 因参与修复已转为回归，不再声称新题验收。 |

计划修复仅在原有严格布尔字段中接入 `backward_index_scan` 并保留摘要，仍核对扫描、连接与排序估算；其他未知字段继续 UNKNOWN。字段含义参照 [MySQL 8.4 EXPLAIN 官方文档](https://dev.mysql.com/doc/refman/8.4/en/explain-output.html)，本地真实计划的对应标记为 `true`。没有为取得通过而更改表权限、风险阈值、数据或既有预期。

该阶段的引用格式调整让模型摘录最短的连续原文，避免复制并拼接整段任务；精确出处校验、完整 SQL 合同和最终复核均保留。总超时及模型请求超时使用固定 TIMEOUT，保存已取得的可信观察；失败后不自动重试。各次源码与输入哈希随报告保存，以上不同源码版本的结果不能合并成一个“最终准确率”。


v4 的固定 SQL 为 40/40（报告 `93c71148950046ce86bac6d74599101e`、`769be59909d24c6f92b2e9e43657335d`），真实新题两轮 16/16（`3aba93ee63d14dcca163ae12274b8cfb`）。旧题回归为 31/32（`ce3f9a99efbf4fec992c05beab35b2a9`）：最后一题生成的 SQL 正确，但模型复述出处时把原文标点改写，因而在执行前停止。这一失败仍计入分母，不当作业务完成。

最终设计取消由模型复述原文的要求，改由运行代码为每份合同绑定实际原始请求的 SHA256。模型不能提供或覆盖可信来源标识，只需产出完整查询要求与未决问题。这样消除复制文字造成的误拦，而不是把错误引用当成正确证据；指纹仅作关联，业务条件仍由独立上下文的需求提取、完整 SQL 编译及最终复核核对，全部权限、SQL 规则与执行预检保持不变。v4 题目、预期和报告保持原样，最终版本使用新的 v5 题目验收。

## 2026-09-11 v5 首轮验收

v5 的 40 道开发题与 8 道新题，在运行前固定题目、参考 SQL、人工预期和产品源码。新题每题执行 SQL 基线 1 次、真实 Agent 2 次，保留所有尝试；以下首轮运行期间未修改产品、原题、预期或数据库。源码证据摘要为 `aaee110a2bfdcea4d06fadd34b79789ea2cbd330677ee0f242a8c7e6bbe26688`，v5 文件 SHA256 为 `59e305d40de21563988804ca5a07c907aadff1d6af3f323f664d399c74d6f8f8`。随后根据这里的计划证据修复 `not_exists` 支持，后续运行仅作已暴露回归。

| 验证 | 实际结果 | 本机报告 ID / 命令 |
| --- | --- | --- |
| 全量测试，开启 MySQL 查询与 DDL 隔离测试 | 1126 项通过 | `DB_AGENT_MYSQL_INTEGRATION=1 DB_AGENT_MYSQL_DDL_INTEGRATION=1 uv run pytest -q --tb=short` |
| v5 开发 SQL，40 题各 1 次 | 40/40 | `27fbbba3af734b1ab354896426683932` |
| v5 开发真实 Agent，40 题各 1 次 | 40/40 | `39686eaeb4584051beaf81ff64360cbc` |
| v5 新题 SQL，8 题各 1 次 | 7/8 | `f29b0e18ae634d6ca74066f632af738e` |
| v5 新题真实 Agent，8 题各 2 次 | 12/16 | `6f9fd16f924a4ce28f52fdbf086360a1` |
| 历史漏 HAVING 候选定向重放，预定 10 次 | 9/10；1 次 TIMEOUT | `bbceb91272e442f09bc403c39672eb4a`；主生成节点固定，需求提取、复核与 MySQL 为真实调用 |

开发回归中的原零订单任务实际生成并执行了 `HAVING COUNT(o.id)=0`，完整返回 `[[4,0],[8,0]]`。定向重放的 9 次完成也均选择带零订单筛选的完整 SQL，只返回该人工预期；另一次在模型请求超时后停止，未派发业务查询。该分母不排除超时，也不冒充完整真实生成。最终版本的组件诊断脚本另修正了超时分类，明确保留 TIMEOUT，不把任意 Agent 错误当成调用次数耗尽；它未参与上述 v5 业务运行。

**新题验收没有全绿。** 四次 Agent 失败都已得到语义 match，但在真实执行入口被计划规则拦住，`execution_status=not_started`、`result=null`；没有把拒绝算成取得业务结果，也没有修改旧 oracle 来获得通过。两类限制分别是：

- **支付退款余额：** 参考 SQL 与两次 Agent SQL 均只用 `refund.payment_id=payment.id` 关联。退款索引和实际复合关系以 `(order_id,payment_id)` 为键，查询没有给退款侧补上订单关联条件；MySQL 8.4.11 在退款表估算扫描 284554 行、连接产出 146140 行，分别触发扫描和排序/临时表的现有 100000 行审核阈值，返回 REVIEW。普通 EXPLAIN 诊断报告为 `ec8e2ff648c6499da3fdee607af4d6c9`。行值预期本身来自正确账本，但题目预填 ALLOW 没有真实计划依据，因此不能将它报告为模型漏筛选或执行器错误放行。补齐关联的等价性验证属于后续 SQL 优化工作，本轮没有改写该题或索引。
- **下过单但从未支付的客户：** 参考 SQL 的聚合后筛选通过；Agent 选择了语义正确的 LEFT JOIN 后 `WHERE o_paid.id IS NULL` 写法，实际计划包含当时解析器未覆盖的 `not_exists: true`，返回 UNKNOWN。受控普通 EXPLAIN 诊断报告 `a676d3860302460cb408372382da1bbc` 确认该表节点估算扫描11行、连接产出9行；问题是字段兼容而非扫描量。未知计划继续停止执行，不能为让任务通过而跳过检查。

这批新题两轮实际模型调用均未超过每题 4 次、工具未超过 4 次，最长任务 12020 ms；这是固定本机合成任务的运行记录，不是延迟保证。请求哈希、完整 SQL 合同和复核没有消除模型共有的理解错误风险；本轮证据支持已知漏筛选问题的修复与执行边界，不能推导任意自然语言任务的正确率或所有合法 SQL 的可执行性。原始报告、运行事件和定向重放材料仅留在忽略的 `outputs/` / `work/`，公开提交只包含代码、合成案例和本文中的摘要。

## 反关联计划兼容修复后的交付回归

`plans.py` 最后仅将 `not_exists` 加入现有严格布尔字段集合，并保留在表计划摘要中。它不表示结果为空，不豁免扫描、连接或排序审核；非法类型、缺少关键估算或其他未知字段仍返回 UNKNOWN。字段来源同时核对 [MySQL 8.4 EXPLAIN 说明](https://dev.mysql.com/doc/refman/8.4/en/explain-output.html) 与 [MySQL 8.4.11 官方 JSON 实现](https://github.com/mysql/mysql-server/blob/mysql-8.4.11/sql/opt_explain_json.cc)。

修复后预定对原 8 道新题做 SQL 1 轮、Agent 2 轮回归，不补跑挑选通过轮次。由于已使用 v5 的计划证据修复产品，当前 v1–v5 全部标为 `exposed_regression`；首次新题 12/16 的报告不变。语义提示、业务合同与预算没有再次调整，v5 JSON 哈希也不变。

| 最终版本验证 | 实际结果 | 本机报告 ID / 命令 |
| --- | --- | --- |
| 全量测试，含两个 MySQL 集成开关 | 1140 项通过 | `DB_AGENT_MYSQL_INTEGRATION=1 DB_AGENT_MYSQL_DDL_INTEGRATION=1 uv run pytest -q --tb=short` |
| Ruff / 工作区及暂存区 diff / 文档相对链接 | 通过 | `uv run ruff check .`、`git diff --check`、`git diff --cached --check`，另逐项核对 Markdown 相对链接 |
| v5 开发 SQL | 40/40 | `a3c920b804104c61a82af4316b049623` |
| v5 原新题 SQL | 7/8 | `d4122a35e267415fbc9aa897265e96af` |
| v5 原新题真实 Agent | 14/16 | `d8632f0db2f44bb08cfd4bd39c17d5c6` |

最终三份业务报告绑定相同源码摘要 `270d4adf0eb499a9de20ddf14779040547b041927f94e033c244629942bc1d40`，实际源码与案例逐文件哈希已回核。客户反关联两次都完成并返回独立预期 `[[5,1]]`，其中一次真实使用了 `WHERE o_paid.id IS NULL` 的计划形态。Agent 每题最多4次模型、3次工具、最长13340 ms；没有提高预算。完整40题真实 Agent 的40/40属于前述语义版本，其后只补计划字段与评测角色标记，不将它冒充最终源码上的重复运行。

**该版本仍未完成的业务任务是支付退款余额查询**：SQL 1次、Agent 2次均因真实扫描风险返回 REVIEW，没有派发或结果。原预填 ALLOW 和失败继续保留。当时可交付完整需求核对、已知漏筛选修复、可信回答与计划兼容修复；[TODO 第11步](../TODO.md) 保持进行中，下一项为退款关联的等价性与计划验证。

## 完整表关系与退款查询验证

`describe_table` 现在返回同库、白名单基础表之间声明的外键，复合关系按约束内的位置返回完整列对；与字段、索引共用原连接、时间、行数及字节预算。实际 reader 取得 `ec_refunds(order_id,payment_id) → ec_payments(order_id,id)`。现有主生成、独立需求提取与最终复核使用这些结构事实，没有增加模型轮数或执行器自动改写规则。显式要求原样执行的 SQL 仍原样进入受控查询服务。

在支付 ID 单独唯一、退款引用列非空且完整引用关系成立的前提下，原 `r.payment_id=p.id` 匹配的每一行也满足 `r.order_id=p.order_id`，补充订单关联保持连接的重复行及后续 SUM/HAVING。13 项独立内存数据库测试分别对照人工整数分预期，覆盖重复退款、拆分支付、全额退款、空集与范围边界，并保留可空引用、支付 ID 不唯一和跨订单脏引用的反例。它们是有限等价性例证，不是通用 SQL 优化证明，也不代表声明过外键就已检查全部历史数据；详见[需求核对](semantic-review.md)。

本机真实 MySQL 的手工候选验证只补充上述订单关联，未改数据、索引、权限或风险阈值：

| 直接受控调用 | 结果 | 报告 / 查询 ID |
| --- | --- | --- |
| 原退款 SQL 普通 EXPLAIN | REVIEW；退款估算扫描284554行 | `0c12bf40c4c941e3922b2704c788692e` |
| 完整关联候选普通 EXPLAIN | ALLOW；退款使用原订单/支付索引，估算扫描8行 | `eb0d497c36bc451db920347699095794` |
| 完整关联候选实际查询 | 完整返回三笔余额，无截断 | 查询 `3c77295f20bf48f992426779ee8a3ed6`；结果 `c83492d6bc5a456e866d5b8efc7a4d71` |

实际行按「支付ID、支付额、成功退款合计、余额」为 `[[23,"50.00","30.00","20.00"],[81,"40.00","20.00","20.00"],[82,"40.00","20.00","20.00"]]`，符合原人工预期。计划行数仍是估算，不能据此报告实测加速倍数。

随后对未修改的 v5 退款业务题预定进行4次真实 Agent 定向回归，报告 `602427d2f508416b92f1102fb4c18973`：3/4完成并返回上述三行，另1次为 `SEMANTIC_REVIEW_INVALID`，未执行 SQL。四份需求合同均生成完整复合关联；失败的原始模型响应未保存，不能推断具体协议字段原因。保留全部四次，不补跑挑绿；这份定向诊断没有将手工候选或参考 SQL 提供给模型。复跑入口为本机忽略的 `work/verify_refund_agent.py`，不属于公开产品命令。

`uv run python scripts/seed_ecommerce.py --verify-only` 同时确认原数据仍为 COMPLETE，六表实际合计6107881行，报告 `load-1368d43e9d9c4bedaa730e6cb5286a4e`。检查包括已匹配退款的订单归属及金额关系，不包含完整孤儿反关联扫描或当前全部约束的完整性审计，未写入数据。最终源码的全量测试开启两个 MySQL 集成开关，1188项通过；`uv run ruff check .` 与 `git diff --check` 通过。

## 2026-09-11 v6 首轮验收与后续修复

v6 将48道已暴露旧题仅改为 dev，另加入8道独立人工构造且复核过的题。首次执行前产品源码快照为 `a96fa266ebd1241aa4ae8dc9f142853c6ec442dce36cd1107886615dd75fc58c`，案例文件 SHA256 为 `851216ec3a32088c394ea990b783d3cc9ebb723ca6f214c0c26adcd0e7beee03`。四组报告的源码摘要均为 `0f6778c5e3b9e882498da115d32a741b7432b7bf5e602207ffd379d3e9184aac`，它还包含锁文件与评测入口；其中每份产品源码与冻结映射一致。

| 首次运行 | 实际结果 | 本机报告 ID |
| --- | --- | --- |
| SQL dev，48题各1次 | 47/48 | `9ae6cf55c70a4f679dd42302e00a68c5` |
| SQL 新题，8题各1次 | 8/8 | `787441bad2924da29d1651422a480bfe` |
| 真实 Agent dev，48题各1次 | 47/48 | `fddbc7fac2c341029d9b7884f0d2c5da` |
| 真实 Agent 新题，8题各2次 | 10/16 | `1655032037054ebc97725f28c4805fa8` |

原退款参考 SQL 仍为 REVIEW、未派发，继续计入 SQL dev 失败；原退款业务题的真实 Agent 已自行通过完整合同选择复合关联，完整返回三笔正确余额。原零订单客户题也在主候选仍漏 HAVING 时，由独立合同选择带 `HAVING COUNT(o.id)=0` 的 SQL 并返回 `[[4,0],[8,0]]`。这两项是完整 Agent 的实际证据，未向模型提供参考 SQL。

本轮没有通过全部业务验收。旧题 Agent 的唯一失败为 `COUNT(DISTINCT...)`，触发既有 `UNSUPPORTED_SYNTAX`。新题“成功支付从未出现任何退款”两轮都生成 NOT EXISTS 子查询，触发既有 `QUERY_SHAPE`；三次都在静态检查返回 UNKNOWN，未进行语义请求或执行。另两道新题各两次因模型调用数耗尽：发现表名→描述结构→生成SQL→独立合同已用4次，最终复核申请第5次时被拒绝。四次耗时6.147–14.334秒、工具4或5次，属于模型轮数限制，不能报告成数据库或请求超时。

针对这些证据，主模型初始上下文补充可信配置中的授权候选表名，明确“存在性、类型和结构未验证”；主模型仍需按任务成批 describe_table，候选名单不进入实际 schemas 或业务合同。此项没有新增数据库预加载、工具或编排图。生成、合同及复核提示同步明确当前 SQL 子集，不生成子查询或 DISTINCT，不简单删掉排除或去重条件。显式要求原样执行的 SQL 仍交给原确定性检查。所有模型、工具、时间和结果预算保持不变。v6 案例与首次失败报告保留，修复后的重跑属于已暴露回归。

四道已暴露失败业务题各2次定向真实回归共8/8，报告 `88e5f2d76dba40c598294787e51e5769`，源码为 `c8d00316a4bf4c0bd74fde3e07605c6da42b54841f3815d2ec642713165ea733`。每次均4次模型、最多4次工具，没有单独发现表名的模型轮次，实际行与原预期一致。本机入口为忽略的 `work/verify_scope_agent.py`；它不向模型传入参考 SQL，也不替代下一轮完整验收。

## 2026-09-11 v7 首轮与数值表示修复

v7 在首次执行前冻结56道旧题、8道独立新题与上述 `c8d00316…733` 源码；案例文件 SHA256 为 `fcd9cfaf0af2d8485dea41eef49e50b8412442a44d5bcafcf01c89e4712e2a9d`。该版本的全量测试1197项通过。四组原始业务报告为：

| 首次运行 | 原始评分 | 本机报告 ID |
| --- | --- | --- |
| SQL dev，56题各1次 | 55/56 | `af24b3b51e754f9bbd73d5d6e03bed46` |
| SQL 新题，8题各1次 | 8/8 | `33665da15f894cd4a402af8ca9f8b2c8` |
| 真实 Agent dev，56题各1次 | 55/56 | `4e53fe71128b40388f3a14273ddacba4` |
| 真实 Agent 新题，8题各2次 | 14/16 | `f35bbb532d1a4c70bf9e2215502a246f` |

SQL dev 唯一失败仍是原退款参考 SQL 的 REVIEW。Agent dev 唯一失败为大扫描原样执行题：最终复核出现 `SEMANTIC_REVIEW_INVALID`，UNKNOWN/error/not_started，尚未进入真实计划判断；耗时7.298秒，不能称为超时或成功拒绝了大扫描。原始模型响应没有保存，不能推断具体格式原因。原退款余额、零订单客户及 v6 暴露的四项业务任务均在本轮真实 Agent 完成。

新题两次失败来自同一个“所有客户的全部/未支付订单数”任务。第一轮生成 `SUM(CASE...)`，被原 `FUNCTION_NOT_ALLOWED` 拒绝，确实未完成查数。第二轮使用 `SUM(o.id IS NOT NULL AND o.paid_at IS NULL)`，真实返回全部8个客户及正确数量，但第三列为 decimal 的 `"0"`/`"1"`，原评测将其与整数预期按 JSON 文本严格比较，造成假阴性。用户没有要求存储类型必须为 COUNT 返回类型；产品按原约定把 Decimal 表示为字符串，这不是业务值或序列化错误。

修复只在生成提示中明确 CASE/IF 不属于现有 SQL 子集，并在评测中增加受限的精确整值比较：实际列必须为 decimal，整列非空预期必须为严格整数；只接受有界的固定十进制整值，不用 float、容差或舍入。小数非零、VARCHAR 数字、布尔值、NULL 差异、错误行宽/顺序/重复仍不能通过，金额和日期等字符串预期保持原精确比较。原结果序列化、SQL规则和所有案例值未改。具体表示上限依据 [MySQL DECIMAL 定义](https://dev.mysql.com/doc/refman/8.4/en/numeric-type-syntax.html)，59项独立数值测试验证该比较边界。

原新题报告14/16保持原样；仅用新比较器对不可变结果离线复评为15/16，衍生报告 `161b2308ff234b3e86f3879b646e6a85` 绑定父报告字节哈希及比较器源码，没有发起模型或数据库请求，第一轮 CASE 拒绝仍计为失败。随后对原未支付订单题预定4次真实 Agent 定向回归为4/4，报告 `2a3ccba3342849efb20c1835625b6847`，每次4次模型、最多4次工具，实际八位客户数量符合原预期；本机忽略入口为 `work/verify_count_agent.py`。最终源码 `81ecadd8fbcd5f1df14e7c52f9953ed7fce7e06a9b64721917d5fc0bbf05ed0d` 的全量测试开启两个 MySQL 集成开关，1257项通过。后续独立验收另用 v8，v7 不再视为未暴露集合。

## 2026-09-11 v8 最终业务验收

v8 在首次运行前固定64道旧题、8道独立新题及上述20文件源码快照 `81ecadd8fbcd5f1df14e7c52f9953ed7fce7e06a9b64721917d5fc0bbf05ed0d`。案例文件 SHA256 为 `aaf285b1f523631f50ab7d06b5c99f96840774555198d988ca4cada21ea4c0d0`，四份正式报告与后续定向诊断均回核到相同源码和案例；首次执行后没有修改产品、问题、参考 SQL、预期、权限或风险阈值。独立推导与冻结过程见 [v8 说明](../evals/V8.md)。

| 预定完整运行 | 实际结果 | 本机报告 ID |
| --- | --- | --- |
| SQL dev，64题各1次 | 63/64 | `3d7e829aad5d4ea18f0c487a4b62ff06` |
| SQL 独立新题，8题各1次 | 8/8 | `4744eea22da84e20a1fd291d6b5ace79` |
| 真实 Agent dev，64题各1次 | 61/64 | `0a8284b952fc46afae93fd880b42f7b5` |
| 真实 Agent 独立新题，8题各2次 | 16/16 | `de8fa1a7be404133b36e237bacde1f6f` |

SQL dev 唯一失败仍是原退款参考 SQL 的 REVIEW，未派发。它保留了当初没有计划依据的 ALLOW 预期，不能据此认定执行器错误，也不能从分母删除。原退款业务题在本轮真实 Agent 自行生成完整关联，实际返回原预期三笔余额；未支付订单计数、v6 的反关联与重复尝试等已知业务失败也均通过本轮回归。参考 SQL 模式与自然语言 Agent 模式分别保留，不能用手工候选的成功代替 Agent。

Agent dev 的三次未完成均保留在61/64中：

- `holdout_no_orders`：第一次模型请求等待30.208秒后 TIMEOUT，没有工具调用或 SQL 派发；这是单次模型请求超时，不是模型调用次数耗尽或数据库执行超时。
- `v2_absent_refund_aggregate`：最终复核返回 `SEMANTIC_REVIEW_INVALID`，UNKNOWN/error/not_started，任务耗时8.072秒。
- `v3_all_east_customer_groups`：同样在最终复核协议检查停止，UNKNOWN/error/not_started，任务耗时9.241秒。

后两次原始模型响应未保存，不能推断是截断、字段缺失还是其他协议问题；评测的 `environment` 分类也不能证明外部服务是根因。三次没有实际查询结果，均不能计为业务查数成功。其余正式 Agent 尝试没有被评测标为数据错误、解释错误或不安全执行；这只是本次固定样本的观察。

随后预定对这三道旧题各做2次真实定向诊断，完整6次为6/6，报告 `a2a099bc39c94a29be29e137233aa8f8`，SHA256 为 `f53b0c9d1236900b6116a75fa04dc1a8dbc4d91f42d4ad4f12e238cce83bf6b1`。仅在忽略的 `work/diagnose_semantic_protocol.py` 包装原解析入口记录安全结构，随后调用原函数；没有修改判断、任务、提示、工具、模型请求或预算。实际查询均完成并由原评测对照独立预期，12份合同/复核响应都是一个有效工具调用、没有解析错误。诊断不保存原始正文、SQL、参数或行值；未复现错误不代表原因已修复，这6次不回填正式分母，也不是新增独立验收。

固定诊断的零订单题两次耗时43.979/53.629秒，仍在60秒总时限内；同一提供方返回的 intent 输出 token 计数为6344/5181，第一次 review 为2057，均超过配置1024。代码通过 `ChatOpenAI(max_tokens=1024)` 请求输出限制，当前没有基于返回 usage 的本地硬 token 上限；提供方计数口径及限制遵从尚未核实。这不能解释成原协议失败由1024截断造成，也不能宣称实际输出或计费已严格限制到1024。模型/工具调用计数和本地时间控制保持原实现，没有提高限制或自动重试。

正式新题16次全部实际完成，最多4次模型、3次工具，最长15.925秒；旧题最多4次模型、4次工具。上述时间是本机合成场景观察，没有生产延迟或任意任务正确率保证。全量测试命令 `DB_AGENT_MYSQL_INTEGRATION=1 DB_AGENT_MYSQL_DDL_INTEGRATION=1 uv run pytest -q --tb=short` 在冻结源码上为1257项通过，Ruff与diff检查通过；包含真实MySQL及独有临时表的事务/元数据锁测试，不能全部称为离线单测。

第11步的有限业务验收已覆盖独立预期、已知业务缺陷回归、新题实际结果和可信失败展示。模型超时、结构化响应不稳定及提供方 token 口径仍是已记录的使用限制，未声称稳定性问题全部消除；后续连续使用与目标环境交付需继续核对。原始报告权限为0600，留在忽略的 `outputs/evals`；临时诊断入口留在 `work/`，均未纳入提交。
